# The Forge — curation shortlist

> Generated by `node scripts/forge-curate.mjs shortlist`. **Mark this file, do not
> hand-edit the corpus.** Then run `node scripts/forge-curate.mjs apply` to write
> `lib/data/forge-review.json`. Re-running `shortlist` preserves marks already applied.

## How to mark

Change the `[ ]` you want to an `[x]`. One per entry. Leave all three blank and the
entry stays `pending`, which renders as **nothing** — the gate fails closed.

| Mark | Means |
|---|---|
| `approve` | Publishable as written. |
| `redact` | Good section, one bad line. Write the replacement under **Redact:**. |
| `never` | Do not publish. Sticks — later re-runs will not re-ask. |

_1062 sections ingested · 415 dropped unreviewed by §4.2 · 647 eligible._

---

## Part 1 — The Scar Room (12 candidates, room ships ~20)

Ranked by whether the section explains a **cause**, not by which class the ingest
guessed. `class === 'scar'` is contaminated, so it counts for two points and
nothing more. **Rejecting six of these is the expected outcome, not a failure.**

### S01 · K53 Drill Master — Technical decisions

`key:0a6a19fc58b7` · class `decision` · 278 words · score 9
<sub>why it surfaced: explains cause, describes a failure, states what it cost</sub>

> **Web Audio API synthesis over audio files.**
> No HTTP requests. No service worker cache entries. Works offline from day one. The synthesized tones took 30 minutes to tune — the `correct` sound needed to feel rewarding without being annoying at 50 plays/session. Final: rising two-note sine wave (660→880hz, 280ms fade).
> 
> ```js
> // The "correct" sound — tuned for repeated listening without fatigue
> osc.frequency.setValueAtTime(660, now);
> osc.frequency.setValueAtTime(880, now + 0.08);
> gain.gain.linearRampToValueAtTime(0.18, now + 0.01);  // attack
> gain.gain.exponentialRampToValueAtTime(0.001, now + 0.28); // decay
> ```
> 
> **`gateCtx.resume()` on every call.**
> iOS and Chrome require AudioContext to be resumed after a user gesture. Every `sfx()` call checks `ctx.state === 'suspended'` and resumes. Without this, sounds silently fail on first tap (iOS PWA).
> 
> **Streak toast at 3/5/7, then every 5.**
> Calibrated to feel achievable (3 is easy) but increasingly exciting (7 = momentum). Beyond 7, fires every 5 to avoid toast fatigue.
> 
> **Score count-up uses ease-out-cubic, not linear.**
> ```js
> const eased = 1 - Math.pow(1 - progress, 3);
> ```
> Linear count-up feels mechanical. Ease-out-cubic starts fast and decelerates — mimics physical counting, feels natural. The score reaches ~95% of its final value by 600ms, then settles for the last 300ms. This is the same curve Stripe uses for their dashboard number animations.
> 
> **`fontVariantNumeric: 'tabular-nums'` on the score display.**
> Without this, the number layout shifts as digits change width (1 is narrower than 8). Tabular numerals use fixed-width digit slots — the container doesn't jump during the count-up.
> 
> **Settings sound toggle: CSS-only, no library.**
> The toggle is a `<button>` with an absolutely positioned white circle. `left` transitions from `3px` (off) to `23px` (on) via CSS `transition`. No state animation library needed for a 48×28px switch.
> 
> ---

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S02 · JarvisOS — The one bug that killed four dashboards

`key:17c04b9c1c67` · class `scar` · 85 words · score 8 · July 29 2026
<sub>why it surfaced: explains cause, states what it cost</sub>

> `supabase.auth.admin.listUsers({ perPage: 1 })` returns users NEWEST-first. The moment the first collaborator account was created (June 29, 14:45 UTC) six workers started resolving *the collaborator* as the tenant — who owns no monitored apps — and silently no-oped. The health-check cron's write rate went 96/day → 0 at that exact minute. That starved the **app health matrix** ("unknown"), **system summary** (dormant), **performance intelligence** (inactive), **engineering alerts**, and most of **Evolution**. Fixed with a shared `OWNER_USER_ID` constant (`src/lib/os/owner.ts`) across health-check, security-scan, sla-report, digest, client-onboard, and sanyu/competitor-snapshot.

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S03 · JarvisOS — Fixed with evidence (selected)

`key:3cc9bd26d8fe` · class `verified` · 565 words · score 7 · July 29 2026
<sub>why it surfaced: explains cause, describes a failure, states what it cost, says how she knew, ⚠ mostly list</sub>

> - **AI PR reviews stale since Jul 2** — the 5 visible rows were SEED data; no PR has ever existed (trunk-based). New `engineering/commit-review` worker: daily 05:15 SAST Sonnet review of the day's REAL commits per repo via GitHub compare diff → same table, card lives daily. Seed rows soft-deleted (added `deleted_at`).
> - **Vercel deployments only showed jarvis** — DEFAULT_PROJECT_MAP guessed slugs that don't exist on the team; corrected to the real project names (admin-os, stokvel-os, nanda-k53-drill-master, sankofa-tv, creatively-nanda-co-za, + sanyubotanicals/mirembemuse added).
> - **Feedback pipeline** — owner's VarsityOS feedback went through VarsityOS's NATIVE form → its own DB, never bridged (engineering_feedback had 0 rows ever; the widget lane itself verified working live, HTTP 201 + auto-triage). Bridged the native route to Jarvis (waitUntil), mounted the widget on MirembeApp, added the direct embed to k53, fixed the CORS bug (POST responses lacked headers → browser showed "Couldn't send" on success), added per-IP rate limiting + a daily AI-triage cap.
> - **CEO "won't let me add a citizen"** — the identity unique index wasn't partial, so a soft-deleted "roy hewett" blocked re-adding anyone with a dead identity. Migration → partial index (`WHERE deleted_at IS NULL`) + revive-on-upsert in contacts.ts + the page now shows the real error.
> - **Duplicate client portals** — the two Jul-2 originals soft-deleted (Selves of Us + BB MotherShip); both live Jul-21 portals kept, terms-acceptance records preserved. Portal health cron no longer resurrects deleted portals.
> - **DB drift** — habits/CWSE/pattern-detect read columns that don't exist on sanctuary_writing_sessions (42703 every run → the "write" habit could never auto-complete); repointed write-habit inference at the REAL writing tables (sanctuary_writing/poems/journal) and aliased words_written. Marketing batch workers updated phantom columns (cycle_phase/content_tone/total_posts) — every batch run silently failed to record context or reach 'ended' (all 172 rows NULL); fixed to live columns with error logging. `app_slo_daily` turned out to be a matview with no refresher → pg_cron nightly refresh scheduled.
> - **content_calendar approval dead end** — new PATCH /api/marketing/calendar/[id] + drafts queue on the approval page + weekly-content-gen now inserts routable slugs/lowercase platforms (and Mirembe Muse joined the weekly roster). Meta lane for brand_posts: scheduled IG/FB posts publish natively at due time WITH their rendered visual; Buffer path now carries mediaUrl.
> - **Monitoring for Jarvis itself** — `os/ops-monitor`: one subscriber to `inngest/function.failed` alerts on ANY worker exhausting retries (covers all ~250 functions), plus a 6-hourly cron-freshness sweep over the key heartbeat tables with a 24h alert latch. Golden-brick harvester (proven working, 887 bricks) gains a weekly cron.
> - **Analytics leverage loop** — apps API now returns per-day series + channel mix + social ROI (synced daily but never selected); dashboard shows sparklines/channel chips; weekly-content-gen injects real GA4 performance into generation; morning brief gains a traffic-anomaly block; new `analytics/weekly-review` worker (Mon 06:00) turns the week's numbers into a high-priority todo + auto-fills content_performance. GA4 failures now log response bodies (the adminos silent-403).
> - **POPIA** — campaign-runner now suppression-checks EVERY step and appends the legal footer; DSR endpoint rate-limited with volume alerts; the 30-day DSR statutory clock is watched daily (nudge d21, critical alert d28).
> - **Corpus capture un-broken** — embedChatExchange self-registers with waitUntil; all 15 chat routes' fire-and-forget calls now actually complete on Vercel.
> - Plus: todos priority ranking (was alphabetical), body training XP + signal merge, streak-90/365 XP, docs gap-scanner scans real gaps (missing file / expired) not "everything older than 7 days", Oracle sees all wings via getWingLiveSnapshot.

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S04 · StokvelOS — 8.1 Five Most Interesting Technical Decisions (LinkedIn Post Starters)

`key:80a8c2ee3440` · class `decision` · 362 words · score 6
<sub>why it surfaced: describes a failure, states what it cost, says how she knew</sub>

> **1. "We replaced 90% of our AI costs with a single architectural decision"**
> The stokvel compliance engine reads your constitution once — extracts 18 rules via Claude — then enforces them forever in pure TypeScript. Zero AI cost per transaction. On 500 members paying monthly, this is the difference between a $47/month AI bill and a $0.05 one. One extraction. Infinite enforcement.
> 
> **2. "South Africa's stokvel members are our 'app' — they never touch the web"**
> We built a web dashboard. Nobody uses it except the chairperson. The 20+ members? They interact entirely via WhatsApp. We built a multi-turn conversational AI agent that handles payments, loan requests, balance checks, dispute filing, and even proof-of-payment image uploads — all inside the WhatsApp app already on their phone. Distribution channel = WhatsApp. Interface = chat. The web app is the back office.
> 
> **3. "We put the entire stokvel constitution into a 90%-cheaper prompt cache"**
> Claude's prompt caching means the first member message from each stokvel pays full price. Members 2 through 30 that hour? ~10 cents each instead of R1.50. In a 30-member stokvel, this drops WhatsApp AI costs from R45/day to R4.50/day. We cache the stokvel constitution, extracted rules, and member context — the stuff that doesn't change turn-to-turn.
> 
> **4. "We gave the AI an ubuntu philosophy — and it resolved 80% of disputes automatically"**
> The dispute agent's system prompt doesn't just say "be helpful." It says: "You are not on anyone's side. You protect the community's trust and harmony. Warm, respectful, ubuntu-centred." Before Claude mediates, an automated investigation compares the claim against 6 months of financial records. Most disputes ("I already paid") are settled by the records alone — the AI just explains what the database shows.
> 
> **5. "Our fraud detector never trusts the balance field"**
> `stokvels.total_funds` is a cached field that can be wrong. Our fraud detection ignores it entirely. We derive the balance from first principles: sum all confirmed contributions, subtract all paid payouts. If the derived balance differs from the recorded balance by >1% AND >R10, it's a critical alert. This pattern — deriving computed fields from source truth at detection time — is how banks do it.
> 
> ---

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S05 · JarvisOS — Deterministic recorder (record.mjs v2, commits 51e08ef+)

`key:0aec382ee586` · class `scar` · 205 words · score 5 · Aug 3 2026
<sub>why it surfaced: describes a failure</sub>

> Playwright's realtime `recordVideo` dropped compositor frames on this laptop → framer springs rendered as glitches. v2 is Remotion-style: virtual clock owns rAF + performance.now, scroll computed per frame in Node, one CDP screenshot per frame, ffmpeg at constant 30fps. **Battle scars, all encoded in the file:**
> 1. `performance.now` must micro-advance per read UNBOUNDED (0.01ms) with peak-tracked monotonic ticks — busy-wait spins hang forever on a frozen clock, and a 30ms crawl cap just moves the hang to any wait longer than the cap.
> 2. CDP `captureScreenshot` returns CSS pixels unless `clip.scale` is set; clip coords are PAGE-absolute so `y` must track scrollY (y:0 = black frames).
> 3. `optimizeForSpeed: true` wedges the capture pipeline a few frames in.
> 4. A 1-device-px damage dot alternating #000/#010101 every tick guarantees the compositor has a frame to hand over.
> 5. Long-delay timers (>5s) are swallowed once recording starts — frame capture stretches ~20s of virtual time over minutes of real time, long enough for timed redirects to destroy the page (VarsityOS /upgrade).
> 6. Frame tmpdirs live in the OS temp dir — OneDrive sync EPERM'd cleanup inside the repo tree.
> 7. Stalled captures fall back to duplicating the previous frame (invisible at 30fps); >60 consecutive dups aborts the page.

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S06 · JarvisOS — Architecture decisions

`key:d00f8881e114` · class `decision` · 151 words · score 5 · May 15, 2026
<sub>why it surfaced: describes a failure, states what it cost</sub>

> **Model routing:** Haiku-ONLY for all body AI (recovery alerts, daily summaries, chat). Body data is structured and formulaic — no Sonnet needed.
> 
> **Recovery score algorithm:**
> ```
> baseline 50
> + HRV contribution: (hrv_ms / baseline_hrv - 1) × 25, clamped ±25
> + sleep contribution: (sleep_hours - 6) × 5, clamped ±15
> + quality contribution: (sleep_quality - 5) × 2, clamped ±10
> final = clamp(50 + hrv_delta + sleep_delta + quality_delta, 0, 100)
> ```
> 
> **External APIs (zero-cost):**
> - Open Food Facts — `https://world.openfoodfacts.org/cgi/search.pl` — no key
> - Wger Exercise DB — `https://wger.de/api/v2/exercise/` — no key, language=2 (English)
> - OpenWeather — East London coords (-33.0153, 27.9116), 3hr UV index + conditions
> 
> **Cross-wing signals:**
> - Body emits → `jarvis:signals:body:{userId}`: `{ recovery_score, sleep_hours, training_sessions, alert, alert_advice }`
> - Body consumes ← `jarvis:signals:cycle:{userId}`: `cycle_phase`, `cycle_day` for phase-aware guidance
> - CEO morning brief extended to read `jarvis:signals:body:{userId}`
> - Cycle daily worker extended to trigger `body/phase.sync` on phase change

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S07 · JarvisOS — The bug that mattered — my own inspector was lying

`key:3a65b615fc42` · class `scar` · 150 words · score 5 · July 27 2026
<sub>why it surfaced: carries the fix, says how she knew</sub>

> Thirteen assets across seven brands flagged "deck truncated", all the same shape. I widened two clamps on that evidence before checking the measurement itself: **`.deck` is a grown flex item (`flex:1 1 auto`), so in a tall panel its `scrollHeight` reports the stretched box, not the text** — a two-line deck measured as 4.87 lines. Every 16:9 module with a tall panel was a false positive. The inspector now releases the stretch (`flex:0 0 auto; height:auto`) before measuring, and **I reverted the deck clamp I had widened on bad evidence**. The headline clamp stays at 4 — that one was a real clip I'd seen in a PNG first.
> - After the fix, six flags survived and were genuine: **a module deck over ~115 characters overflows the 16:9 panel**. Trimmed the copy rather than widening the clamp — the format is the constraint. Written into `samples-brands.ts` as a content law.

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S08 · K53 Drill Master — Technical decisions

`key:8cee24ac15e1` · class `decision` · 138 words · score 5
<sub>why it surfaced: describes a failure, states what it cost</sub>

> **PDF extraction methodology.**
> ```
> Page 3  → images 10–16:  Stop (R1.1=010), Yield (R2=016)
> Page 4  → images 17–25:  No Entry, Speed limits
> Page 22 → images 272–285: Warning crossroads/junctions
> Page 26 → images 329–343: Pedestrian/traffic warning signs
> ```
> Used pdfjs `paint operation count` to isolate which images were actual signs vs decorative elements. Signs have a characteristic square/circle bounding box ratio.
> 
> **395 images, not lazy-loaded at page load.**
> *Why:* Questions are randomised. Can't predict which images will be needed. Instead: images are small JPEGs (~8–15KB each), loaded on-demand when a question is rendered, cached by service worker after first view.
> 
> **`onError` SVG fallback:**
> ```jsx
> <img
>   src={`/signs/sign_${q.img}.jpg`}
>   onError={e => { e.target.style.display='none'; setShowSvg(true); }}
> />
> {showSvg && <SvgSign type={q.svgType} />}
> ```
> *Why:* Some extracted images are corrupted. Rather than audit 395 files, let the browser detect failure at runtime.
> 
> ---

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S09 · JarvisOS — Battle scars (all encoded in the scripts now)

`key:297d23fa4d69` · class `scar` · 126 words · score 5 · Aug 2–3 2026
<sub>why it surfaced: describes a failure</sub>

> 1. `page.screenshot` hangs forever on infinite CSS animations — `animations:'disabled'` + `bypassCSP` fixed VarsityOS and Sanyu.
> 2. My own corner-widget stripper ate K53's ENTIRE bottom nav on mobile (390px nav < 420px "small widget" threshold) — width cap is now 72% of viewport.
> 3. K53's Exam view hides the bottom nav — tab taps must start from the dashboard every time.
> 4. VarsityOS heavy pages OOM-crash shared renderers on this laptop — fresh browser context per shot, full-page capture only for the dashboard.
> 5. Live schema drift bit twice: `tasks.task_type` allows 'reading' not 'study'; `part_time_jobs` wants `employer_name`/`pay_rate`/`job_type='tutoring'` (Apr 2026 migration, NOT the Jan one).
> 6. `/demo` and `/tour` on VarsityOS are auth-gated (not in middleware publicRoutes) — public captures of them are really the login page.

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S10 · StokvelOS — 1. The Problem That Triggered the Build

`key:6d9b74b923b8` · class `scar` · 167 words · score 4
<sub>why it surfaced: describes a failure, states what it cost, ⚠ mostly list</sub>

> South Africa has an estimated **11 million stokvel members** managing over **R50 billion annually** — mostly in WhatsApp groups, paper ledgers, and shared Google Sheets. A typical stokvel chairperson juggles:
> 
> - Tracking who paid this month (and who hasn't)
> - Manually calculating late penalties from a handwritten constitution
> - Sending individual reminders to 15–30 members via WhatsApp
> - Resolving disputes ("I sent the money last Tuesday!") with no paper trail
> - Writing meeting minutes from voice notes
> - Detecting when someone is trying to defraud the group
> 
> The original trigger: a chairperson of a 20-member teachers' stokvel lost **R4,200** because two members paid to an old account, the records weren't reconciled, and by the time the discrepancy was found 3 months had passed. There was no audit trail.
> 
> **The core insight:** A stokvel is not a bank. It's a trust community with rules. The problem isn't missing money — it's missing transparency. Fix the transparency, and most fraud, disputes, and misunderstandings dissolve before they become crises.
> 
> ---

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S11 · CreativelyNanda — Signature Generation and the Sort Bug

`key:2de3304d5e05` · class `entry` · 139 words · score 4
<sub>why it surfaced: describes a failure, carries the fix</sub>

> PayFast authentication relies on an MD5 signature of all form fields. The original implementation of `generateSignature()` in `lib/payfast/index.ts` sorted the keys alphabetically before building the parameter string:
> 
> ```typescript
> // BROKEN — sorted alphabetically
> const params = Object.keys(data)
>   .sort()
>   .filter(key => key !== 'signature' && data[key] !== '')
>   .map(key => `${key}=${phpUrlencode(data[key])}`)
>   .join('&');
> ```
> 
> PayFast verifies the signature using the POST field order it receives from the browser, which matches insertion order in the JavaScript object, not alphabetical order. The `.sort()` call caused a field order mismatch, generating an incorrect signature and resulting in payment failures.
> 
> The fix was to remove `.sort()` entirely. The current `createPaymentData()` function builds the data object in PayFast's canonical field order (merchant details, URLs, buyer details, transaction details) and `generateSignature()` preserves that insertion order. This was the single most critical bug fix in the entire project.

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

### S12 · BB MotherShip — Test framework + first suite (gap: zero tests)

`key:1767b8b5ffcb` · class `entry` · 75 words · score 4
<sub>why it surfaced: describes a failure, states what it cost</sub>

> - **vitest** installed; `npm test` → `vitest run`. `vitest.config.ts` maps the
>   `@/` alias, node environment.
> - First suite targets the pure, high-risk utils that had real bugs historically:
>   `lib/utils/sast-date.test.ts` (the UTC+2 conversions behind past off-by-a-day /
>   wrong-hour bugs — bounds, hour, mocked-clock rollover), `xp.test.ts` (rank
>   thresholds + progress caps), `pin.test.ts` (hash round-trip, wrong pin, unique
>   salt, malformed-input never-throws). **16 tests, all green.**
> - RLS/integration tests still need a test DB — documented as a follow-up.

**Redact:** _(leave blank unless marking redact)_

`[ ] approve`  `[ ] redact`  `[ ] never`

---

## Part 2 — Tier 1 remainder (60)

The judgment core minus what Part 1 already took: decisions, audits, receipts, lessons.
These become pull-quotes threaded through the wing. Tick a row to approve it sight-unseen,
or leave the table alone and I will bring you the bodies in a second pass.

| ✓ | key | app | class | words | heading |
|---|---|---|---|---|---|
| `[ ]` | `846d6fb25878` | AdminOS | decision | 184 | Stack Decisions |
| `[ ]` | `da361eae97da` | AdminOS | verified | 104 | Trial + Langa |
| `[ ]` | `03c2437bdab5` | AdminOS | audit | 84 | DB schema mismatches fixed (mobile audit) |
| `[ ]` | `e4e79fc75e54` | BB MotherShip | decision | 231 | Engineering decisions made |
| `[ ]` | `181f14aafd1f` | BB MotherShip | decision | 200 | Engineering decisions made |
| `[ ]` | `b1737492e9c1` | BB MotherShip | decision | 164 | Engineering decisions made |
| `[ ]` | `aed809a48d30` | BB MotherShip | decision | 141 | Engineering decisions made |
| `[ ]` | `b7aa08852a17` | Campus Compass | verified | 133 | The Bigger Picture |
| `[ ]` | `23f1a47bc7bd` | Campus Compass | verified | 117 | Landing Page — Full Redesign |
| `[ ]` | `d81b657aea79` | Campus Compass | audit | 68 | Pricing Restructure (cost audit) |
| `[ ]` | `db6e279c11f3` | Campus Compass | verified | 62 | Monetisation roadmap |
| `[ ]` | `282da91c958a` | CreativelyNanda | audit | 159 | Component Inventory |
| `[ ]` | `e01059d04c66` | CreativelyNanda | verified | 126 | Webhook Handler |
| `[ ]` | `ce79cf2505a7` | CreativelyNanda | decision | 75 | Architecture decisions |
| `[ ]` | `961284f3bf4e` | Foundations | lesson | 233 | The Throughline |
| `[ ]` | `7266fb27a4df` | Foundations | scar | 132 | The Build |
| `[ ]` | `f99390eb3232` | Foundations | lesson | 102 | The Lesson |
| `[ ]` | `8687f02609e3` | Foundations | lesson | 100 | The Lesson |
| `[ ]` | `2f5ec7c63594` | Foundations | lesson | 100 | The Lesson |
| `[ ]` | `821eff7b7d25` | Foundations | lesson | 98 | The Lesson |
| `[ ]` | `5ec886e73263` | Foundations | lesson | 97 | The Lesson |
| `[ ]` | `0ab7183eae9b` | Foundations | lesson | 95 | The Lesson |
| `[ ]` | `1ca0d5460a8b` | Foundations | decision | 86 | The Build |
| `[ ]` | `aad3563e6ba0` | Foundations | lesson | 84 | The Lesson |
| `[ ]` | `6f641bc88127` | Foundations | lesson | 73 | The Lesson |
| `[ ]` | `f25687d23f57` | Foundations | lesson | 72 | The Lesson |
| `[ ]` | `33d0f3f83fe2` | JarvisOS | decision | 171 | Per-account command centers (owner request: "each account its own page") |
| `[ ]` | `20862418124a` | JarvisOS | decision | 162 | Library cleanup (owner request, session close) |
| `[ ]` | `6c4f5ca87c96` | JarvisOS | decision | 138 | Key design decisions |
| `[ ]` | `c5378871b6ae` | JarvisOS | decision | 128 | Architecture decisions |
| `[ ]` | `8c7792b13da5` | JarvisOS | lesson | 125 | The rules that make it work |
| `[ ]` | `840c7ab4ed4c` | JarvisOS | verified | 122 | P2 — HTML → PNG (headless Chrome) — VERIFIED |
| `[ ]` | `e890bf1a79ae` | JarvisOS | scar | 117 | Gotchas / lessons |
| `[ ]` | `170cf962db23` | JarvisOS | decision | 110 | Architecture decisions |
| `[ ]` | `22aa02e8ee7a` | JarvisOS | decision | 103 | Architecture decisions |
| `[ ]` | `d293a87eaec3` | JarvisOS | decision | 102 | Key design decisions |
| `[ ]` | `55906be581b2` | JarvisOS | audit | 100 | 3 New Inngest Workers |
| `[ ]` | `a7d9cbbe4580` | JarvisOS | decision | 99 | Architecture decisions |
| `[ ]` | `f5107c465d5d` | JarvisOS | decision | 88 | Dedup pass (owner: "remove duplicate pages", commit f9eaa93) |
| `[ ]` | `f451c11fed52` | JarvisOS | decision | 87 | Architecture decisions |
| `[ ]` | `04868d31ae06` | JarvisOS | decision | 81 | Creatively Nanda captured (owner authorised) |
| `[ ]` | `196b1c001cb3` | JarvisOS | decision | 78 | Architecture decisions (Phase 12) |
| `[ ]` | `919c47d2d1e6` | JarvisOS | scar | 73 | Gotchas / lessons |
| `[ ]` | `f0990d071a9a` | JarvisOS | audit | 70 | The audit |
| `[ ]` | `c62a3b77bb50` | JarvisOS | scar | 65 | Gotchas / lessons |
| `[ ]` | `9f645caecec8` | JarvisOS | lesson | 62 | Dashboard — Language Studio |
| `[ ]` | `23e0658748d6` | K53 Drill Master | decision | 216 | Technical decisions |
| `[ ]` | `d8502aec6242` | K53 Drill Master | decision | 210 | Technical decisions |
| `[ ]` | `3b11cfcd9579` | K53 Drill Master | decision | 147 | Technical decisions |
| `[ ]` | `9afd11fa0b85` | K53 Drill Master | decision | 103 | Technical decisions |
| `[ ]` | `c6f0ea501ddf` | K53 Drill Master | decision | 96 | Technical decisions |
| `[ ]` | `b2197f916c45` | K53 Drill Master | decision | 81 | Technical decisions |
| `[ ]` | `ba3d52fd7049` | K53 Drill Master | decision | 72 | Technical decisions |
| `[ ]` | `fe927f1af67c` | K53 Drill Master | decision | 69 | Technical decisions |
| `[ ]` | `44f20d8ac255` | TrueAccess | audit | 108 | Admin: edit venues + extra audits (`ccc8931`) |
| `[ ]` | `d602813eb8e0` | TrueAccess | lesson | 107 | Core Design Principles (non-negotiable) |
| `[ ]` | `6125edabb1e3` | TrueAccess | verified | 104 | 🔴 Two CRITICALs (verified) |
| `[ ]` | `9c38ed8af257` | TrueAccess | scar | 78 | Web SEO + Open Graph (`2024e1b`) |
| `[ ]` | `7d6bca282eba` | WatchSankofa | decision | 161 | Community Layer |
| `[ ]` | `3ddb674718a0` | WatchSankofa | decision | 69 | Table of Contents |

## Part 3 — Tier 2, build context (103)

Vision, architecture, phases and receipts — the Workshop Floor spine. Lower risk
(these describe systems, not incidents) and higher volume.

| ✓ | key | app | class | words | heading |
|---|---|---|---|---|---|
| `[ ]` | `099f90d90223` | AdminOS | shipped | 263 | Shipped |
| `[ ]` | `9aeb00c94d17` | AdminOS | architecture | 217 | Git Commit History |
| `[ ]` | `b69d53507446` | AdminOS | architecture | 179 | Phase 2 — WhatsApp Engine |
| `[ ]` | `e98a30a654ca` | AdminOS | architecture | 168 | Phase 2 — WhatsApp Engine |
| `[ ]` | `095b798f6bcd` | AdminOS | phase | 148 | Phase 7 — SEO & Performance |
| `[ ]` | `777cdd2e5661` | AdminOS | architecture | 130 | Workflow Engine Architecture (v2) |
| `[ ]` | `be0cd251d042` | AdminOS | architecture | 121 | Repo study → blueprint (`ADMINOS_OS_ARCHITECTURE.md`) |
| `[ ]` | `d1003e2514ef` | AdminOS | shipped | 116 | UX overhaul — 4 issues fixed |
| `[ ]` | `bfe34ecb5a28` | AdminOS | shipped | 102 | 9 missing Inngest functions built |
| `[ ]` | `7b93a1b95014` | AdminOS | phase | 102 | Phase 6 — Onboarding |
| `[ ]` | `454d191cc7d7` | AdminOS | architecture | 94 | Billing — one data-driven model (single source of truth) |
| `[ ]` | `3776e4e223d1` | AdminOS | shipped | 77 | v2 Strategy — Closing Every Loop |
| `[ ]` | `2f7bc9e031ee` | AdminOS | architecture | 76 | Multi-Tenant Architecture |
| `[ ]` | `306530a0990e` | AdminOS | shipped | 75 | Reported blockers — fixed & deployed (commit `9a43285`) |
| `[ ]` | `5fccd58a4347` | BB MotherShip | shipped | 171 | Phase 2 milestones delivered |
| `[ ]` | `19c5fcda2794` | BB MotherShip | shipped | 141 | Phase 1 milestones delivered |
| `[ ]` | `d973c28d3669` | BB MotherShip | shipped | 116 | Validation rolled out to 35 server actions |
| `[ ]` | `0f7de7c646dd` | BB MotherShip | architecture | 107 | Dashboard redesign (the flagship) |
| `[ ]` | `e6dde7984188` | BB MotherShip | shipped | 92 | Phase 3 milestones delivered (first wave) |
| `[ ]` | `c369e3056fd2` | BB MotherShip | shipped | 82 | C1 — Cockpit → HACCP command view + oil status |
| `[ ]` | `c0f4df104095` | BB MotherShip | architecture | 69 | Input validation layer (gap: no validation) |
| `[ ]` | `acd8e7eddb36` | Campus Compass | architecture | 94 | Current Stack |
| `[ ]` | `50434ef7eec7` | Campus Compass | shipped | 65 | Nova Intelligence Upgrades |
| `[ ]` | `e0d9b123d214` | CreativelyNanda | shipped | 245 | What was built |
| `[ ]` | `0e5666b1b83b` | CreativelyNanda | phase | 244 | 13. What Was Learned |
| `[ ]` | `cce55c3f8b37` | CreativelyNanda | vision | 113 | The Vision |
| `[ ]` | `33b4c17f59c3` | CreativelyNanda | shipped | 112 | JSON-LD Generators |
| `[ ]` | `576f2e630270` | CreativelyNanda | phase | 79 | 9. Multi-Language Support |
| `[ ]` | `3b033ded8422` | CreativelyNanda | architecture | 79 | Zustand for Cart State |
| `[ ]` | `d8241198cffd` | CreativelyNanda | shipped | 74 | Homepage — Magazine Cover Experience |
| `[ ]` | `f3b443e4a816` | CreativelyNanda | vision | 72 | PayFast over Stripe |
| `[ ]` | `f0c7c80d6017` | Foundations | architecture | 156 | The Build |
| `[ ]` | `d4ec95385e43` | Foundations | architecture | 114 | The Learning Architecture |
| `[ ]` | `b86144bfb96d` | Foundations | vision | 109 | Overview |
| `[ ]` | `873b7c48012b` | Foundations | shipped | 104 | The Build |
| `[ ]` | `9cd7eec3c9b9` | Foundations | vision | 85 | The Brief |
| `[ ]` | `0d8b41d866cf` | Foundations | shipped | 84 | What Was Built |
| `[ ]` | `e2d92ca70802` | Foundations | vision | 64 | The Brief |
| `[ ]` | `09369e7f34bb` | JarvisOS | shipped | 425 | Shipped |
| `[ ]` | `8ac052f5ef6f` | JarvisOS | shipped | 282 | The template finish line (this session's build) |
| `[ ]` | `47cfacfa9cb2` | JarvisOS | shipped | 224 | Shipped (`src/lib/marketing/cta-catalog.ts`) |
| `[ ]` | `cf70dec37321` | JarvisOS | shipped | 218 | What got built (not just fixed) |
| `[ ]` | `039e0dc2b055` | JarvisOS | shipped | 218 | Gaps addressed |
| `[ ]` | `10dacff81a40` | JarvisOS | shipped | 195 | Session close (Aug 3, ~02:45) |
| `[ ]` | `af48b2c84b80` | JarvisOS | shipped | 185 | What shipped |
| `[ ]` | `1328abde1a07` | JarvisOS | shipped | 182 | Component (`src/components/goddess/GoddessSidebar.tsx`) |
| `[ ]` | `0dc0bbc56cb3` | JarvisOS | vision | 151 | What & why |
| `[ ]` | `eb2c060ea156` | JarvisOS | shipped | 150 | Shipped |
| `[ ]` | `a24393531fe8` | JarvisOS | shipped | 149 | DB Schema Gaps Fixed |
| `[ ]` | `1f3bcd210752` | JarvisOS | architecture | 142 | Growth/Funnel cockpit — make the loop visible + shout when a pipe is dead |
| `[ ]` | `3eb53e1a1a42` | JarvisOS | shipped | 139 | Framed plates (deployed, commit caaf2f2) |
| `[ ]` | `b589f94c4670` | JarvisOS | architecture | 134 | B1 Launch Pricing Strategy (`schemas.ts`, `production.ts`) |
| `[ ]` | `d2fe743aa54c` | JarvisOS | shipped | 128 | Bugs Fixed |
| `[ ]` | `5cfbe99d3ee2` | JarvisOS | architecture | 125 | Dashboard rebuild (`src/app/dashboard/marketing/page.tsx`) |
| `[ ]` | `4c68c0aac0d9` | JarvisOS | shipped | 123 | Shipped |
| `[ ]` | `c33be9a35cf9` | JarvisOS | phase | 113 | 3 — Auto follow-ups (the conversion multiplier) |
| `[ ]` | `a6c0c6ac670b` | JarvisOS | vision | 104 | Dashboard additions (`src/app/dashboard/consulting/page.tsx`) |
| `[ ]` | `a4a8c8954e99` | JarvisOS | architecture | 103 | AI architecture |
| `[ ]` | `c1d5d894f77f` | JarvisOS | phase | 96 | 3 · Per-post performance pull (`marketing/performance-pull`, Sun 19:00 SAST) |
| `[ ]` | `a0e83fcb626a` | JarvisOS | architecture | 92 | How it works |
| `[ ]` | `c72385fd04e5` | JarvisOS | architecture | 92 | Architecture — 4 Corpus Layers |
| `[ ]` | `b59423fd982c` | JarvisOS | phase | 89 | 1 — Bulk import (fixes the driving-school OSM gap) |
| `[ ]` | `e82cb6f3be92` | JarvisOS | architecture | 89 | Architecture constants (Wing 12) |
| `[ ]` | `d1f7ac213530` | JarvisOS | phase | 87 | 2 — Journalist finder (VarsityOS press — people, not places) |
| `[ ]` | `0a04c7701ebe` | JarvisOS | architecture | 87 | Architecture constants (Wing 10) |
| `[ ]` | `3ffc45b514fb` | JarvisOS | shipped | 85 | `src/app/dashboard/marketing/posts/page.tsx` — Generate fix + phase-aware |
| `[ ]` | `dee817387c81` | JarvisOS | shipped | 83 | Fixed (commit 224dd8a, deployed) |
| `[ ]` | `4b844337667e` | JarvisOS | shipped | 83 | Shipped |
| `[ ]` | `8d007db1c651` | JarvisOS | phase | 77 | 1 · Social inbox poller (`studio/inbox-poll`, every 30 min) |
| `[ ]` | `52be11cab1d5` | JarvisOS | architecture | 77 | Architecture constants |
| `[ ]` | `6de4f8dd92f0` | JarvisOS | architecture | 76 | Architecture constants (Wing 10 v2) |
| `[ ]` | `444ee15fba67` | JarvisOS | shipped | 73 | The cadence — 5 a day, in daylight |
| `[ ]` | `151deaca02bb` | JarvisOS | architecture | 72 | Architecture Notes |
| `[ ]` | `a0d8e6944f48` | JarvisOS | shipped | 71 | UI Changes |
| `[ ]` | `089a7438130b` | JarvisOS | phase | 70 | 2 · IG carousels + Reels publish (`meta.ts`) |
| `[ ]` | `9b8bc3bd164a` | JarvisOS | vision | 70 | What & why |
| `[ ]` | `6683693e0eaa` | JarvisOS | vision | 70 | What & why |
| `[ ]` | `c8a24e56f08f` | JarvisOS | vision | 63 | What & why |
| `[ ]` | `907512277791` | JarvisOS | architecture | 63 | Wing 15 — SankofaGrow + Crisis Centre |
| `[ ]` | `e64ee8886d9f` | JarvisOS | vision | 62 | What & why |
| `[ ]` | `71eb81299e08` | JarvisOS | architecture | 62 | Architecture constants (Wing 11) |
| `[ ]` | `ced1e7fff9a9` | K53 Drill Master | shipped | 294 | What shipped |
| `[ ]` | `4e9bef2fda79` | K53 Drill Master | shipped | 263 | What was built |
| `[ ]` | `6a97930d5f6d` | K53 Drill Master | architecture | 86 | Analytics stack |
| `[ ]` | `cecd888dcfcf` | K53 Drill Master | shipped | 83 | What shipped |
| `[ ]` | `381224be792a` | K53 Drill Master | shipped | 64 | What shipped |
| `[ ]` | `1c9b2a2667a2` | K53 Drill Master | shipped | 63 | What shipped |
| `[ ]` | `e7142b02b386` | StokvelOS | phase | 259 | 10.3 Static Asset Generation — OG Image, Favicons, PWA Icons |
| `[ ]` | `6892922ae5d5` | StokvelOS | phase | 254 | 8.2 Three Problems Most Developers Overlook |
| `[ ]` | `bf195a1552b2` | StokvelOS | phase | 224 | 10.4 Operator Documentation — /ops and /troubleshooting Pages |
| `[ ]` | `50c0df644510` | StokvelOS | phase | 170 | 8.3 Two Moments Where the Build Surprised Us |
| `[ ]` | `5f01f2e8e690` | StokvelOS | phase | 159 | 8.4 A Poem Starter — The Human Problem This App Solves |
| `[ ]` | `23faa7c1e372` | StokvelOS | phase | 137 | 9.1 The Four-Tier Structure |
| `[ ]` | `59dcc3ba37c5` | StokvelOS | phase | 110 | 9.3 WhatsApp Fair Use Limit |
| `[ ]` | `14e4c8fb0935` | StokvelOS | phase | 100 | 4.5 Monthly Health Report Agent |
| `[ ]` | `173afba5e5ed` | StokvelOS | phase | 99 | 9.2 AI Cost Coverage Analysis |
| `[ ]` | `14f24e3cfea0` | StokvelOS | phase | 85 | 3.3 Rate Limiting (Upstash Redis + in-memory fallback) |
| `[ ]` | `2918987d6621` | StokvelOS | phase | 80 | 6.1 Prompt Caching (biggest cost win) |
| `[ ]` | `30e6030aa284` | TrueAccess | vision | 104 | The Vision |
| `[ ]` | `b0ed4d513d82` | TrueAccess | vision | 87 | Problem Statement |
| `[ ]` | `3a67dfd9e1bf` | WatchSankofa | architecture | 346 | AI Agent Architecture |
| `[ ]` | `6989150e91f3` | WatchSankofa | shipped | 145 | AI Features Built |
| `[ ]` | `8ad08a894219` | WatchSankofa | vision | 79 | Vision & Mission |

---

## Not shown, and why

Dropped wholesale by §4.2 — **415 sections**, no review offered:

| Rule | n |
|---|---|
| `class === 'manifest'` — env vars, route lists, DB config | 122 |
| `class === 'security'` — live exposure write-ups | 65 |
| `sensitive === true` — keys, client names, figures | 295 |

The `security` exclusion is permanent by decision, not an oversight: those apps are
still running. The wing gets **one hand-written "Security posture" essay** instead —
the class of bug and the systemic fix, no live specifics. See spec §4.6.
